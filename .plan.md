# 天命殿 · 功德换轮回加成/难度

## 问题

天命功德点是死资源：

- 只有一个消耗口（轮回殿 8 个一次性特质），总价 275 点。
- 收入远超支出：任务链到飞升真仙累计发 6450 点，每次轮回另加 `10 + (exp + realmIndex*150)/40`。
- 玩两三世就买断全部特质，之后功德无限堆积、永远无处可花。
- 8 个特质全部只在**出生时**生效（`btnConfirmCreate` 内），中途赚的功德当世不可用。

## 方案

新开独立「☯️ 天命殿」面板，功德在此购买**轮回加成**与**难度倒券**——每次转世生效、可反复投入的元进度，而非一次性道具。

保留现有轮回殿与 8 个特质不动（避免破坏存档与既有平衡），天命殿作为并列的第二套消耗口。

### 一、可叠加轮回加成（DESTINY_BOONS）

多级、成本递增，功德永远有去处。等级存 `META.boons = { id: level }`。

| id | 名称 | 每级效果 | 上限 | 成本公式 |
|---|---|---|---|---|
| `boon_qi` | 宿慧吐纳 | 月均吐纳 +8% | 20 | `12 + lv*8` |
| `boon_life` | 龟龄玄息 | 基础寿元 +15 岁 | 10 | `18 + lv*12` |
| `boon_break` | 破境道韵 | 突破率 +2% | 10 | `25 + lv*15` |
| `boon_wealth` | 财帛天授 | 开局灵石 +200、凡银 +150 | 10 | `10 + lv*6` |
| `boon_body` | 淬体宿根 | 开局体魄/神魂 +15 | 10 | `15 + lv*10` |
| `boon_luck` | 紫微气运 | 机缘事件触发率 +6%（相对） | 8 | `30 + lv*20` |

满级总消耗约 6900 点 —— 与任务链总产出同量级，长期有目标但不至于遥不可及。

### 二、轮回难度倒券（DESTINY_TRIALS）

反向消耗：**花功德提高难度**，换取当世永久增益倍率。开局前选定，存 `META.trial`。

| id | 名称 | 代价 | 增益 | 解锁价 |
|---|---|---|---|---|
| `trial_none` | 太平顺世 | 无 | 无 | 0（默认） |
| `trial_mortal` | 凡胎逆仙 | 开局锁四灵根杂品、寿元 -20 | 功德结算 ×1.6 | 40 |
| `trial_demon` | 魔劫加身 | 心魔每月自增、突破率 -8% | 功德结算 ×2.2 | 80 |
| `trial_shortlife` | 朝露之命 | 寿元上限砍半 | 功德结算 ×3.0 | 150 |

倒券是**永久解锁**，解锁后每世可自由切换。这给功德一个"投资换加速"的循环，而非单纯堆数值。

## 实现

单文件 `index.html`，全部改动集中在 6 处：

1. **数据层**（~3270 行，`META_PERKS` 之后）：新增 `DESTINY_BOONS`、`DESTINY_TRIALS` 常量与 `boonLevel(id)`、`boonCost(id)`、`trialMult()` 辅助函数。
2. **META 结构**（3274 行）：加 `boons: {}`、`trial: "trial_none"`、`unlockedTrials: ["trial_none"]`。因为 `loadMeta` 用 `{...META, ...p}` 展开，旧存档缺字段会自动落回默认值，向后兼容无需迁移代码。
3. **面板 HTML**（1368 行后，轮回殿 modal 之后）：新增 `modalDestiny`，复用现有 `.modal-overlay` / `.inv-list` / `.btn-purple` 样式。侧栏在 `btnOpenReincarnation` 旁加 `btnOpenDestiny` 入口。
4. **渲染与购买**（~5850 行，`buyMetaPerk` 附近）：`renderDestinyModal()`、`buyBoon(id)`、`selectTrial(id)`、`unlockTrial(id)`，沿用 `saveMeta()` + 重渲染的既有写法。
5. **效果挂载点**（4 处窄改动）：
   - 吐纳/气运：`executeCurrentRoutine` cultivate 分支（3888 行 `baseGain`、3874 行机缘 `Math.random() < 0.05`）
   - 突破率：`perkBonus` 累加处（5985 行）
   - 出生数值：`btnConfirmCreate` 内 `lifeBonus` / `startStones` / `basePhy` / 难度券锁灵根（8863–8940 行）
   - 心魔自增：`advanceMonth`（7254 行）加魔劫券判定
6. **功德结算**：`settleKarmaAndReset`（5867 行）`earnedKarma` 乘 `trialMult()`，并在结算提示中显示倍率来源。

不碰 20 处分散的 `G.stones +=`，所以本方案不含全局掉落倍率——那需要先抽出统一的 loot helper，属独立重构，不放进本次改动。

## 验证

无测试框架（纯静态单文件）。用浏览器手动验证：

1. 起本地服务，开 `modalDestiny`，确认加成列表渲染、成本递增、功德不足时按钮禁用。
2. 买 1 级宿慧吐纳 → 转世 → 确认月均吐纳数值与日志随等级提升。
3. 解锁并选定朝露之命 → 转世 → 确认寿元减半、死亡结算功德约 3 倍。
4. 用旧存档（无 `boons` 字段）载入，确认不报错、面板按默认 0 级渲染。
5. 检查 console 无报错。
